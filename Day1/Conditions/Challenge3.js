let prompt = require('prompt-sync')();
            let a=prompt("please enter your grade :") ;
            let b ;
            if 
                (a>=10)
            {b="admis"}
        else {b="non admis"}   
        console.log("resultat : "+b) ;