
             let prompt = require('prompt-sync')();

             let a=prompt("please enter a number :") ;
            let b ;
            if (a>0)
              {b="Le nombre est positif."} 
           else if (a==0)
              {b="nombre nul"} 
           else b="Le nombre est négatif" 
        console.log(b) ;
       