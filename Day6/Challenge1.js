const student={
    firstName: "sassy" ,
    lastName: "lezmate",
    age: 22 ,
    city: "otah"  ,
    school: "Uiz"
}
console.log(`Prénom : ${student.firstName}`)
console.log(`Nom : ${student.lastName}`)
console.log(`Age : ${student.age}`)
console.log(`Ville : ${student.city}`)
console.log(`${student.firstName} ${student.lastName} is ${student.age} ans et habite à ${student.city}`)